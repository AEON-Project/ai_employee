# CHANGELOG

每次完成开发后必须更新本文件（按 [docs/ai/WORKFLOW.md §C](../ai/WORKFLOW.md#c-完成开发) 约定）。

格式：

```markdown
| <commit hash> | <一句话内容> | <对应文档链接> |
```

跨多个 commit 的工作单元用 `abc1234..def5678` 范围标记。

---

## V1.0 引擎验证版

| commit | 内容 | 对应文档 |
|---|---|---|
| `b584234` | initial: V1.0 引擎验证版（α + β + W0–W4 共 45 工单一次性落地） | [ALPHA_TASKS §0](./ALPHA_TASKS.md) |
| `824382c` | docs(alpha-tasks): 标记开发状态 | — |
| `f3aefa7` | docs(readme): 重写 README | — |
| `11b812b` | fix(cli): `backup` 命令父目录缺失 | — |
| `65ff4ae` | feat(config): 支持 `.env` 配置（Bun 自动加载，三层覆盖） | [README §3.1](../../README.md) |
| `1900da7` | feat(credentials): `env://` 引用协议（modelKeyRef） | — |
| `1ad538c` | feat(env-ref): `env://` 扩展到 model/baseUrl 字段 | `.env.example` |
| `527153e` | feat(cli): `./ai-emp` shell wrapper | [README](../../README.md) |
| `aee5b93` | style: prettier 自动换行 config.ts 长行 | — |
| `67581e6` | docs(readme): 快速开始改走 .env 路径 | — |
| `f69a6f6` | feat(cli/init): 下一步提示上下文感知 | — |
| `663d4cc` | feat(seed): 5 员工角色（后端/前端/测试/产品/UI 设计）+ 输出清晰化 | — |
| `dcf573b` | fix(seed): `--reset` 真删（不是 archive） | — |
| `f0d8f43` | docs(claude): 加 CLAUDE.md | [CLAUDE.md](../../CLAUDE.md) |
| `6ade242` | docs(claude): CLAUDE.md 加产品需求 + 进度状态 + AI 协作工作流 | [CLAUDE.md](../../CLAUDE.md) |
| `d644706` | docs(claude): 回填 6ade242（吃自己狗粮验证闭环） | — |
| `11a36ec` | docs(claude): 加"本地端到端调试（浏览器自动化）"段 — browser_navigate MCP | [docs/ai/DEBUGGING.md](../ai/DEBUGGING.md) |
| `48631d4` | docs(claude): 回填 d644706 + 11a36ec 到"已交付"表 | — |
| `964f6ac` | docs: 按文件夹分类整理文档 + 拆分 CLAUDE.md（新增 docs/{product,architecture,progress,ai}/） | 本文件 + [WORKFLOW](../ai/WORKFLOW.md) + [DEBUGGING](../ai/DEBUGGING.md) + [CONVENTIONS](../architecture/CONVENTIONS.md) |
| `fb888a3` | feat(web): 补员工/项目/技能详情页 — 填齐 α 阶段 Web UI 占位（员工技能挂载多选+记忆 Tab / 项目规范+知识 CRUD / 技能编辑器）；后端补 PATCH/DELETE skills、PATCH employees、POST memory items | [PRD V1 §M1-M3](../product/PRD_V1.md#m2-员工能力载体) |
| `0f93758` | fix(server): HTTP 派单后调 scheduler.enqueue — 修复 executeRequirement 永不触发（α 阶段 server 模式 P0 bug；e2e 测试一直绕过 server 直调 execute 所以未覆盖） | — |
| `9992f8c` | feat(web): 补「需求」总列表页 + 项目详情页「需求」Tab（顶栏 #/requirements 表格视图+9 状态筛选；后端 GET /requirements 支持 status/projectId/all 组合查询） | [PRD V1 §M4](../product/PRD_V1.md#m4-需求任务入口) |
| `0a00156` | fix(ux): LLM 错误/system_pause 在 UI 思维链可见 + Controls 加 busy/err 显示（"点击继续无响应"根因：systemPause 没 append error message + Controls 吞错误） | — |
| `6cf9253` | docs(debugging): 更新 playwright MCP 全流程测试模板（工具速查/e2e 启动/三条交叉证据/常见 pitfall） | [DEBUGGING.md](../ai/DEBUGGING.md) |
| `4fbea13` | feat(logging): 结构化 NDJSON 日志体系（packages/domain/logger + 埋点 HTTP/scheduler/LLM/SQL + 脱敏 + 「日志先行」工程纪律） | [DEBUGGING.md §3-6](../ai/DEBUGGING.md) |
| `5b6b3f8` | fix(runtime): ask_user args 健壮化 + dispatch 抛错走 systemPause 不冒泡（日志一行定位 args.questions.map TypeError；状态从卡死「进行中」→ 优雅落「已暂停」可恢复） | — |
| `c1e1db4` | docs(debugging): e2e 自动化测试加「杀旧 server 启新进程」流程（lsof -ti :7878 -sTCP:LISTEN 比 pgrep 可靠 + EADDRINUSE pitfall） | [DEBUGGING.md §2](../ai/DEBUGGING.md) |
| `484b92b` | fix(prompt): 给 LLM 暴露 ask_user 等系统 tool 的真实 JSON Schema（ToolDef 加 inputJsonSchema；composer prompt 加结构化提示）— playwright 验证：gpt-4o 正确产出 trigger_reason=missing_info + 2 个 question 数组 → 状态进「等待回答」+ 澄清卡片渲染 | — |
| `412e1ab` | fix(runtime): streaming delta 合并为单条 message（修复思维链「逐字竖排」bug）— handleChunk 改用 StreamingBuffer，按 thinking/text 类型累积，类型切换 / tool_use_stop / message_stop / error / 循环末尾兜底时一次性 flush；中文 provider 把流切到 1–3 字粒度时不再每字一条 | — |
| `09bc043` | feat(web/thread): 思维链 seq 倒序展示（最新在顶）+ 滚动到底 sentinel 触发分页加载更早历史 + 每条消息加 HH:MM:SS；server `?limit=N[&beforeSeq=X]` 分页接口 + storage `MessagesRepo.pageByThread`（hasMore via limit+1） | — |
| `6555d2d` | fix(runtime): advance_step 标 plan.step.status=done + 累积 historySummary（不再清空）+ resumeRequirement 复位 budgetUsed — 修复 LLM 反复 update_plan / 空 advance_step 直到撞 budget_iterations 的死循环（日志定位 reqId 86fc597a 30 轮内 4 次 update_plan + 17 次 textLen=0 的 advance_step）；3 个回归测试 | — |
| `eb5bd5e` | fix(prompt+runtime): composer 注入 currentStep + historySummary（注释 §13/§14 早就写了但实现遗漏，LLM 一直看不到自己干了啥）+ advance_step 的 currentStep 严格单调递增（max(ctx+1, completedIdx+1)，防 LLM 反复报旧 step_idx 卡步号）— playwright E2E 闭环：重启后 resume → 自动答澄清 → 5 步 plan 全 done → status=待验收（iter 27/30 cap，deliverable 已写）| — |
| `d9099e9` | feat(tools): V1.1 file/shell tool 集 — Read / Write / Edit / Glob / Grep / Bash 完整本地权限 — 单用户本地引擎决策：不做路径白名单，等同于运行 server 的用户的终端权限。工程保留：输出截断 50000 字符 / Bash 超时 + AbortController SIGKILL / NDJSON 审计日志 (scope=tools.file)。E2E: 让员工真改 /Users/yuanyong/work/lskj/virtual_card_api/CardChannelTypeEnum.java 加 NEW_BANK_ALCHEMY 枚举值 6 轮 LLM 调用完成。18 个单测 + 211 总测全过 | — |
| `58395e2` | fix(prompt+runtime): V1.2 防 LLM 路径幻觉 + 工具失败假装完成 — (1) composer 加文件 / Shell 工具使用规则（Edit 前必须 Glob/Grep 探索；ok=false 时严禁 advance_step）；(2) runtime advance_step dispatch 前扫最近 tool_result，ok=false 则硬阻止（不更新 plan/step/history，写 system/error 让 LLM 下轮看到）。E2E 第 4 次派发 5 步 plan 11 轮跑通到「待验收」（blocked=0，LLM 改用 Glob/Grep 探索后无 ENOENT 失败）—— 但暴露 V1.3 候选：LLM 0 次 Edit 就 emit_deliverable 谎报"已修改 4 文件" | — |
| `d1cd040` | fix(runtime+prompt): V1.3 emit_deliverable 防"谎报完成" — extractClaimedFilePaths(text) regex 抓 30+ 后缀的 file path 候选；collectEditedPathsFromThread() 扫 tool_result 收集 Write/Edit ok=true 的 value.path；任何 claimed 路径找不到 Edit/Write 记录 → 拒绝交付 + 写 system/error。composer 加规则"summary 只能写真改过的文件，引擎会对账"。runtime.test 加 2 用例：声称无证据 → 拒绝 / 有 ok=true 的 Edit 记录 → 放行。E2E 第 5 次派发 gpt-4o 因 OpenAI TPM 30k 限流 + Edit 多次 old_string 多匹配冲突未跑通，但 V1.2 advance_step.blocked 触发 2 次拦下"工具失败后假装推进"。232 pass。| — |
| `c4ad1f9` | fix(runtime): V1.4 LLM 调用临时错误自动退避重试（429/5xx/网络），不再立即 system_pause — 任何 LLM 调用错误就 system_pause 让人接管违反"AI 数字员工"语义。execute.ts 加 analyzeLlmError(msg)（识别 429 + 提取 retry-after / 5xx / ETIMEDOUT / ECONNRESET / fetch failed），retryable=true 时 log.warn('llm.retry') + sleep(delay) + continue 重进 for loop；ExecuteOptions.maxLlmRetries=5 默认，成功一轮后归零；try/catch 整个 for-await stream 覆盖 provider 直接抛错路径。3 个回归用例：429 退避 → 待验收 / 401 永久错误 → 暂停 / 重试用完 → 暂停。235 pass。| — |
| `0d45d1a` | feat(tools): V1.1 简化为纯 Bash 透传 — 删除 Read/Write/Edit/Glob/Grep 5 个包装工具。用户语义澄清：要的是"大模型调本地命令行执行任务、引擎做透传"，包装工具反而限制 LLM 灵活性（Edit 不会用 replace_all 自救、Glob 1000 限制等）。FILE_TOOLS 只剩 Bash 一个；composer 协作规则改"cat/sed/find/grep/brew/mvn 自由组合"。V1.3 对账配套改：dispatch default 分支增 append type='tool_call' 落 Bash command 文本，collectModifiedPathsFromBashHistory 扫所有 ok=true+exitCode=0 的 Bash 命令文本提路径 token 与 claimed path 对账。file-tools.test 10 用例（含 cat/sed/find/grep 场景）；229 pass。| — |
| `ee728f8` | revert(runtime): 拆 V1.3 emit_deliverable 对账（借鉴 /Users/yuanyong/work/aeon/OpenClaw 设计） — 员工提交工作直接进「待验收」，由用户验收时看 git diff 鉴别真假。删除 execute.ts unverified 拦截 + 删除两个 helper（extractClaimedFilePaths / collectModifiedPathsFromBashHistory）+ composer prompt 改"如实汇报，谎报会被 reject"。删除 5 个 V1.3 对账用例，加 1 新用例验证 emit 不拦截。E2E 第 6 次派 25 轮 LLM 调用直接到「待验收」（之前 V1.3 拦着撞 maxLoops=50 死循环）；保留 V1.2 advance_step.blocked + V1.4 LLM retry；228 pass。| — |
| `fe47ebd` | feat(web): 「待验收」自动显示 Git Diff — 配合 V1.3 拆除：UI 拉 git status/diff 让用户一眼鉴别员工是否真改代码。storage projects.workdir 字段（migration 0002） + ProjectsRepo 接受；server GET /requirements/:id/git-diff（spawn git，status/stat/diff 各 200KB 上限 + 10s 超时）；web RequirementDetail 在「待验收」插 GitDiffPanel：no_workdir 提示去配置/hasChanges=false 红字"可能谎报建议 reject"/有改动则展示 stat+完整 diff（100 行折叠）。playwright 验证 b9e16577（员工 0 改动）显示警告。227 pass。 | — |
| `3a680ab` | feat(web): Git Diff Panel 用 diff2html 做 GitHub Dark 风格 split view — 用户反馈多文件分隔不清晰，希望点击文件看左右对比。引入 diff2html@3.4.56（CSS +17KB / JS +303KB），Diff2HtmlUI 配置：drawFileList + fileListToggle（顶部文件列表） / fileContentToggle（每文件可折叠） / outputFormat 切换 side-by-side ↔ line-by-line（按钮）/ matching:lines + highlight 行内 inline diff / synchronisedScroll / colorScheme: dark；store/router.ts useRoute 监听 hashchange 忽略 #d2h- 前缀（diff2html 用 hash 做文件锚点跳转会触发 404 路由）。playwright E2E d69a81fb（LLM 真改 enum +6 -22）渲染完美 GitHub Dark 左右对比。| — |
| `1eff507` | feat(tools+prompt): Bash 加 env / background / yield_ms + 新增 Process tool + composer 防路径幻觉规则 — 借鉴 OpenClaw exec 设计。Bash args 新增 env（临时环境变量） / yield_ms（默认 30000：长命令转后台不再被 SIGKILL） / background（立即后台）。新 Process tool（standard）：read/status/kill 后台 sessionId。in-memory Map 维护 ProcessSession。composer 加"防路径幻觉"段：每个 path 在 Edit 前必须 find/ls 验证；ENOENT 立刻退回 find；不裁剪目录前缀。9 个新单测覆盖 env/background/yield/Process 各场景。238 pass。| — |
| `07dcbfe` | docs(progress): ALPHA_TASKS §14 V2 优化清单（OpenClaw 对比后整理）— 7 个缺陷 D1-D7（按 ROI 排序，P0：sub-agent 缺失 / memory 闭环弱）+ 6 个优化项 O1-O6（按工程量/价值比）+ 不做边界（保留工单状态机护城河，不抄 pi-agent-core，不接 20+ IM）+ 推进建议（O2 memory 闭环 → O1 sub-agent → O3 PTY → O4 prompt cache）| [ALPHA_TASKS §14](./ALPHA_TASKS.md#14-v2-优化清单openclaw-对比后整理--2026-05-27) |
| `1a1ee56` | docs(progress): §14 重构为三方对比终版（OpenClaw + hermes-agent + ai_employee）— 加入 [hermes-agent](https://github.com/NousResearch/hermes-agent)（Nous Research，Python 3.11 + SQLite FTS5，self-improving agent）二轮对比，揭示 4 个 §14 漏掉的高价值缺陷：D1 Skills 自演化（hermes agentskills.io 自创生自改进，PRD §1「下次更好」最强实现）/ D4 Checkpoint 回滚（hermes checkpoint_manager.py，非 git 项目安全网）/ D5 Cron 定时工单（hermes cron/jobs.py，「日报巡检周清理」）/ D6 MCP client（hermes optional-mcps/，工具生态一夜十倍）。原 §14（OpenClaw 单方）+ §15（hermes 扩展）合并成单一「§14 三方对比终版」：§14.1 三方定位差异表（15+ 维度）/ §14.2 D1-D13 缺陷清单按 ROI 重排（P0 升级为 O1 Skills / O2 memory / O3 sub-agent）/ §14.3 O1-O13 优化项每项明确修复哪个 D / §14.4 5 波次推进顺序 / §14.5 不做边界（不变成 hermes / OpenClaw / 不拆 40+ 细分 tool / 保留 9 态状态机 + 待验收 Git Diff 护城河）| [ALPHA_TASKS §14](./ALPHA_TASKS.md#14-v2-优化清单--三方对比终版2026-05-27) |
| `0e1431e` | feat(memory+tools): V2 O1 Skills 自演化系统 — 三方对比第一波 — 参考 hermes-agent agentskills.io 让 LLM 完成任务后主动沉淀"可复用做法套路"到员工长期记忆；下次同员工接到相似任务 composer 按相似度 Top-K 自动注入到 system prompt。最小侵入设计：复用 memory_items 表（MEMORY_KINDS 加 'skill'，schema enum 同步 domain/events/server/storage 四处）+ 现有 RAG 基础设施（reindexSource → chunks → vec_chunks）。新增 emit_skill 系统 tool（args: name + whenToUse + steps[] + triggers[]，runtime dispatch 写 memoryItems(scope=employee, kind=skill) + 走向量索引；缺字段写 system/error 不暂停主流程，旁路沉淀哲学）；composer ③RAG 增 skills 召回（scope=employee）+ 注入"⭐ 优先按 skill 步骤执行不要从零摸索"；协作规则告 LLM 何时调 emit_skill（"一类可复用套路"vs"一次性具体修改"）。4 个新测试：emit_skill 正确参数 / 缺字段 / RAG kind='skill' 命中 / scope=employee 隔离。241 pass。| [ALPHA_TASKS §14.3 O1](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `f7a2433` | feat(memory): V2 O2 memory 闭环强化 — PRD §3「纠错沉淀」核心机制最后一环。原本 facts/pitfalls/lessons 表 + composer RAG 注入都已就位但 reject 路径只做状态转移、不沉淀；员工被驳回也学不到东西。本次补两条沉淀路径形成闭环：① **LLM 主动** — 新增 emit_lesson 系统 tool（args: content + scope('employee'\|'project') + context?），runtime dispatch 写 memory_items(kind=lesson) + reindex；缺 scope/project 时降级 system/error 不暂停（旁路沉淀同 emit_skill）；composer 协作规则告 LLM 何时调（察觉错误/反复失败/澄清里被指出错/弯路警觉）。② **用户被动** — rejectRequirement 改 async 接 opts.reason?；reason 非空时 thread 落 system text + 写 employee.lesson("用户驳回时反馈：X（来自工单"T"）") + reindex；emit memory.persisted。server POST /reject 接 body.reason；bridge-tg 加 await；Web UI ApprovePanel 用 window.prompt 收原因。闭环：下次同员工相似需求 → composer §③ lessons RAG 自动 Top-K 注入。5 个新测试。246 pass。| [ALPHA_TASKS §14.3 O2](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `e05ea62` | feat(runtime+tools): V2 O3 sub-agent 协作 — spawn_employee 系统 tool — PRD「组织+岗位」心智完整性，第二波 P0 / §14.3 O3。父员工把子任务派给另一员工，引擎同步执行子工单后把子员工 deliverable 作为 tool_result 回传，父员工拿到结果继续干（"组织"心智第一次能跑起来）。storage: 0003_requirement_parent.sql 加 parent_requirement_id（self-reference）+ req_parent 索引 / RequirementsRepo.create 接受 parentRequirementId。tools: spawnEmployeeTool（targetEmployeeId + taskTitle + taskDescription）。runtime dispatch: ①缺字段 → system/error 不暂停 ②防递归 (parentRequirementId 非空时拒绝，深度 ≤ 1) ③防自环 (targetEmployeeId == 自己) ④找不到 target ⑤createReq(parentRequirementId=reqId, projectId 继承, 复用 budget) ⑥assignRequirement(skipClarification=true) ⑦await executeRequirement(subReqId) 嵌套 ⑧抓子 thread 最后 assistant text 作 summary ⑨写 tool_call+tool_result 到父 thread (ok=subExit==='delivered') ⑩父 continue。子有独立 BudgetTracker，token 不计父。composer 协作规则警告"不要把整个工单 spawn 出去 — 那只是甩责任"。4 个新测试。250 pass。 | [ALPHA_TASKS §14.3 O3](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `ec9195e` | feat(checkpoint+runtime+web): V2 O4 Checkpoint 回滚 — 工单快照与一键 revert — 安全网：非 git 项目 / 数据文件改坏 / LLM 谎报"已修改"都能一键回到工单开始的样子，第二波 P1 / §14.3 O4。git + tar 双后端覆盖"项目是 git 仓库"和"项目是任意文件目录"。storage: 0004_checkpoints.sql 新表 (id / req_id / kind(baseline\|manual) / label / backend_kind(git\|tar\|none) / ref / workdir) + index；CheckpointsRepo + _setBackendRef。core/checkpoint/index.ts 新模块：detectBackend(workdir) → git/tar/none；snapshot() → git rev-parse HEAD 拿 SHA，tar 模式 tar czf 到 <ckptDir>/<reqId>/<ckptId>.tar.gz (排 node_modules/.git/dist)；revert() 先 tar 备份当前到 preRevert-<ts>.tar.gz 再硬恢复 (git reset --hard + clean -fd；tar 模式 readdirSync 删后 xzf 恢复)。runtime/execute.ts 主循环前 auto-baseline (有 checkpointsDir + 顶层工单 + 无既有 baseline)；dispatch case 'checkpoint' (LLM 主动建 manual)；checkpointsDir 缺/snapshot 异常 → system/error 不暂停。runtime/commands.ts revertToCheckpoint(services, ckptId) + thread 留痕。services.ts 加 checkpointsDir?: string。tools/builtin.ts checkpointTool (label string 1-80)。composer 协作规则加 checkpoint 描述。server/api.ts POST /reject 加 body.revertCheckpointId（先 revert 再 reject）/ GET /checkpoints / POST /checkpoints/:id/revert。cli/services.ts 自动注入 dataDir/checkpoints。web/RequirementDetail.tsx ApprovePanel reject 流程加可选 ckpt 回滚（prompt 选序号 + confirm）。8 个新测试（detectBackend ×3 / tar snap+revert / git snap+revert / none ×2 / auto-baseline / 无 ckptDir 跳过 / checkpoint tool / revertToCheckpoint）。261 pass。 | [ALPHA_TASKS §14.3 O4](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `a7c7490` | feat(cron+runtime+web): V2 O5 Cron 定时工单 — 「日报/巡检/周清理」自动化，第三波 P1 / §14.3 O5。cronSpec 非空的 requirement 是"模板"（永不被 dispatch），scheduler 每 60s 扫一次到期就创建一份 child 工单派给同一员工。storage: 0005 加 cron_spec/cron_enabled/cron_last_run_at 字段 + 部分索引 req_cron WHERE cron_spec IS NOT NULL。core/cron 新模块：parseCron 解析 "every N minutes/hours" / "daily HH:MM" / "weekly mon\|...\|sun HH:MM"（不实现完整 5-field cron，大小写不敏感乱写返 null）+ CronSpec.nextAt() + shouldTriggerCron()。scheduler.bindServices 保存 services 引用；startCronTicker(60s)/stopCronTicker(); cronTick(now) 遍历 listCronTemplates → parseCron → shouldTriggerCron → 创建 child (cronSpec=null, parentRequirementId=tpl.id, title 追加"定时触发") + assignRequirement(skipClarification=true) + setCronLastRun + enqueue。server POST /requirements 接 cronSpec/cronEnabled + 创建前校验语法（→ 400 invalid_cron_spec）。cli/serve 启动 cron ticker 关闭时 stop。web/NewRequirement 加 "定时（可选）" 输入框 + cronSpec 非空时强制指派。20 个新测试（cron parser 15 / runtime cron tick 5）。281 pass。 | [ALPHA_TASKS §14.3 O5](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `2100db8` | feat(prompt+llm): V2 O8 prompt cache 精细化 — 三段独立 breakpoint — 第四波 P1 / §14.3 O8。把 system prompt 切成"平台/项目/需求"三段独立缓存：①平台层 (persona+style+skill 后) → 跨项目稳定 ②项目层 (项目规范后) → 跨需求稳定 ③需求层 (RAG 注入后) → 同需求多轮稳定。原实现只 1 个 bp 切换工单就全失效。composer.ts 加 computeJoinedBytes + dedupeMonotonicBreakpoints (去重+单调递增+上界裁剪)；ComposedPrompt.cacheBreakpoints 从 1 段升 [1..3] 段。llm/anthropic buildSystemBlocks 改支持多 bp（清洗 + 上限 4 段 ephemeral 符合 Anthropic 规范）；N 个 bp → N+1 个 text block，前 N 段 cache_control:ephemeral，尾段不标。新测试 2 个（三段 bp 递增 + 平台/项目内容分段 / 无项目仍至少 1 段）。292 pass。 | [ALPHA_TASKS §14.3 O8](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `3c7e175` | feat(tools+cli): V2 O9 Process notify-on-exit — 后台进程退出主动通知 — 第四波 P2 / §14.3 O9。后台 Bash 进程 close 时自动写 system message 到 thread + 工单"进行中"时 scheduler.enqueue 唤醒 LLM，让 LLM 不需要 sleep+轮询 Process read 也能知道 mvn/npm install 结束。tools/file-tools: ProcessSession 加 requirementId/threadId/notified/backgrounded 字段；setProcessExitNotifier(fn) 全局回调注入点；child.on('close') 在 backgrounded=true && 未 notified 时 → 调 notifier({sessionId, reqId, threadId, command, status, exitCode, durationMs}); yield 转后台路径设 backgrounded=true（前台同步路径永不通知）。cli/commands.ts serve 启动 scheduler 后 setProcessExitNotifier(handler): 写 "⏱️ 后台进程 <sid>... 已结束 — 状态=X exitCode=Y 耗时=Zs" + 状态"进行中"时 scheduler.enqueue。3 个新测试（背景退出触发 / 前台同步不触发 / 抛错不影响主流程）。295 pass。 | [ALPHA_TASKS §14.3 O9](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `f0027e4` | feat(tools): V2 O10 危险命令审批 — 黑名单拦截 + env 跳过开关 — §14.4 第四波 P2 最后一项 / §14.3 O10。最小可工作版本：黑名单匹配 → exitCode=126 + stderr 含 DANGEROUS_COMMAND_BLOCKED 详情；LLM 看到自然会调 ask_user 拿授权或改命令。完整 WS 双向审批 UI 留 V2.X。新增 checkDangerousCommand() 函数覆盖：rm -rf 根/系统/家目录 / 提权 (sudo/doas/su) / 网络脚本 pipe shell (curl\|sh) / dd 写块设备 / 格式化 (mkfs/fdisk/...) / chmod 777 / fork bomb / 关机重启。env AIEMP_ALLOW_DANGEROUS=1 全局跳过。4 个新测试（15 场景黑名单识别 / 正常命令放行 / Bash invoke 拒绝 / env 跳过）。300 pass。§14 全部 O1-O10 落地完成 🎯 | [ALPHA_TASKS §14.3 O10](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `46b9c83` | feat(trajectory+cli+server): V2 O11 Trajectory dump — 工单 thread 导出为 OpenAI chat 格式 — §14.4 第五波第一项 / §14.3 O11。给用户调试 / 备份 / 分享工单完整对话历史（不是为了训练 LLM — §14.5 边界明确不做 hermes trajectory_compressor 训练管道）。core/trajectory 新模块：extractTrajectory(repos, reqId) 转 OpenAI chat 格式（role + content + tool_calls + tool_call_id）；thinking/text → assistant content (thinking 加 [thinking] 前缀)；tool_call → assistant + tool_calls 数组；tool_result ok=true → tool role + content (string 直接 / object JSON)；ok=false → "[error] ..."；plan_update → "[plan_update]" 前缀；clarification_answer → user Q/A；clarification_request → "[ask_user]"；system/error → "[error]"。toJsonl() 第一行 __meta__ + 每条 message 一行 JSONL。server GET /requirements/:id/trajectory（Accept: application/x-ndjson 或 ?format=jsonl）。cli: ai-emp trajectory <req-id> [--jsonl]。6 个新测试（空 thread / 完整 thread 5 类型映射 / error / system/error / 找不到工单 / toJsonl 格式）。306 pass。 | [ALPHA_TASKS §14.3 O11](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `2530e6f` | fix(runtime+prompt): V2 P0 修复 — emit_deliverable / advance_step 拦"上一步工具失败" + composer extractText 完整序列化 tool_result — V2 业务端到端验证（KYC 注册接口）发现 2 个互相关联的 P0 bug。**bug 1**: composer.ts extractText 把 tool_result 序列化成字符串 "tool_result" 丢失所有内容 → LLM 在 chat history 看不到上轮 stdout 反复跑同样命令（git status × 8 死循环）。修复：抽 ok/value.status/value.exitCode/value.stdout(2000)/value.stderr(500) 拼可读字符串；m.role==='tool' → role='user' + "[tool_result]\n..."；同时支持 tool_call / error / clarification_request 序列化。**bug 2**: V1.2 advance_step.blocked 只看 ToolExecutor 层 ok=false，看不到 Bash 内部 status='failed' / exitCode≠0 → sed 失败但 ok=true，advance_step 通过；emit_deliverable 完全没拦截 → 直接转"待验收"谎报。修复：抽出 detectLastToolFailure(repos, threadId) 公共函数（同时检查 ok=false / value.status='failed' / value.exitCode≠0 / value.isError=true）；advance_step 改用；**emit_deliverable case 新增同样拦截**（防"工具失败 → 假装完成"P0 漏洞）。2 个新回归测试（Bash exitCode≠0 advance_step 被拦 / emit_deliverable 也拦上一步命令失败）。**端到端业务验证**：派 KYC 注册工单给小后员工，4 轮：①旧引擎 0 真改动谎报 ②V2 sed 失败谎报（暴露 P0 bug）③修复后真改 KycController 但凭直觉发明类名→reject+revert ④lessons 注入起作用 LLM 真探索 13 Bash + 真跑 mvn（2 次失败被拦 9 次），budget 用完暂停但 workdir 全程干净。证明 V2 安全网完整：lessons RAG / baseline auto / emit_deliverable.blocked / revert / preRevert backup 全工作。308 pass。 | [V2_E2E_VERIFICATION](../ai/V2_E2E_VERIFICATION.md) |
| `b491a57` | docs(C): 整理 V2 文档 — 新增 PRD_V2 + V2_E2E_VERIFICATION + 更新 README/GETTING_STARTED/CLAUDE — V1+V2 文档体系完整版。新增 PRD_V2（~165 行，6 组 11 项能力 + 不做边界 + O12/O13 处置 + 工程统计）；新增 V2_E2E_VERIFICATION（~280 行，11 个端到端验证场景含 SQL/curl/jq 检查 + 验证结果记录模板）；README 加 V2 8 能力简介 + 状态 306 tests；GETTING_STARTED 加"V2 新能力速览"含 cron 语法/mcp.json 示例/危险命令开关；CLAUDE 文档导航加 PRD_V2 + V2_E2E_VERIFICATION。next: A 端到端真实验证（用户按 playbook 跑 11 场景，发现 bug 定位修复） | [PRD_V2.md](../product/PRD_V2.md) + [V2_E2E_VERIFICATION.md](../ai/V2_E2E_VERIFICATION.md) |
| `4e0aa4e` | feat(mcp-client+cli): V2 O6 MCP client — 接入 Model Context Protocol 工具生态 — 工具生态一夜十倍，第三波 P1 / §14.3 O6。新包 @ai-emp/mcp-client 极简实现 stdio MCP 协议（initialize 握手 + tools/list + tools/call，不实现 resources/prompts/sampling）。**McpClient**: spawn 子进程 + NDJSON JSON-RPC 2.0；connect 握手 protocolVersion 2024-11-05；listTools 缓存；callTool 返回 {isError, content[]}；close SIGTERM + 等 exit 1s 兜底。**McpManager**: 多 server 管理 + fqn 索引；connectAll 并发连接（失败不阻断）；callByFqn 路由；closeAll 并发关闭。**CLI 集成**: config.ts 加 mcpConfigPath/loadMcpConfig（dataDir/mcp.json，schema {servers:[{name,command,args,env?,startupTimeoutMs?}]}，不存在→空配置不报错）；services.ts bootServices 启动 MCP，自动注册 ToolDef(kind='standard', inputSchema=passthrough)，standardToolNames 合并 FILE_TOOL_NAMES + mcpToolNames（默认对所有员工授权）。工具命名 mcp_<server>_<tool> 防冲突。root tsconfig + cli deps 加 mcp-client + zod 显式依赖。7 个新测试（inline mock MCP server via bun -e + tmp file）：echo connect+list+call / initialize 报错 / 不回响应超时 / 找不到命令 / connectAll 多 server / 部分失败不阻断 / callByFqn 未知 fqn。288 pass。 | [ALPHA_TASKS §14.3 O6](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `1bcef69` | feat(tools): V2 O7 PTY 支持 — Bash 工具加 pty:true 走伪 tty — mvn/gradlew/npm install/git rebase 等检测 isTTY 的命令能正常输出。第四波 P1 / §14.3 O7。借用系统自带 `script` 命令做 pty 包装避免 node-pty native binding 兼容性问题。BashArgsZ 加 pty?: boolean；macOS: `zsh -c 'script -q /dev/null /bin/sh -c "<cmd>" < /dev/null'`（< /dev/null 必须，否则 script 抱怨 stdin）；Linux: `script -q -c "<cmd>" /dev/null < /dev/null`；Windows fallback 到普通 spawn + warn。3 个新测试（pty=true → [-t 1] TTY_YES / pty=false TTY_NO / echo 完整路径）。限制：macOS script 输出头有 ^D；不实现 stdin 交互（单向 PTY）。291 pass。 | [ALPHA_TASKS §14.3 O7](./ALPHA_TASKS.md#143-全部优化项按-roi-排序) |
| `b41f320` | fix(web): EmployeeDetail 编辑页加 modelBaseUrl 字段 — 补 V2 端到端验证发现的 UI 漏字段 — V2 真实业务验证时发现：员工配置改成 openai-compat + 自定义 base URL 时，UI 编辑页 BasicTab 缺 modelBaseUrl 输入框，只能通过 sqlite 直改或调 PATCH API。types.ts Employee 加 modelBaseUrl: string \| null；EmployeeDetail BasicTab form 加 modelBaseUrl 字段（空字符串 → null 存库）；UI 提示"OpenAI 兼容协议自定义端点，留空走 SDK 默认；可用 env://AIEMP_OPENAI_BASE_URL"。 | — |
| `9fd305b` | docs: ALPHA_TASKS §15 — V2 端到端真实业务验证总结（4 轮 KYC 实战 + 3 个 P0 bug + V2 11/11 能力验证） — 真实 Java 项目（virtual_card_api）KYC 注册接口开发 4 轮端到端测试：①旧引擎 0 真改动谎报 ②V2 sed 失败谎报（暴露 P0 bug 1+2）③修复后真改 KycController 但凭直觉发明类名→reject+revert ④lessons 注入起作用 LLM 真探索 13 Bash + 真跑 mvn（2 次失败被拦），budget 用完暂停 workdir 全程干净。3 个 P0 bug 全修：composer.extractText 完整序列化 / advance_step+emit_deliverable 拦 bash 内部失败 / UI modelBaseUrl 字段。V2 11/11 能力实战验证（O1 skills / O2 memory 闭环 / O4 checkpoint revert / 自演化 / 拦截谎报）。教训：LLM 模型能力是真实业务质量上限（gpt-4o 写 Java 凭直觉发明类名）；unit test pass 不等于 E2E 可用，必须真业务 + 真 LLM 跑通才能发现 P0。 | [ALPHA_TASKS §15](./ALPHA_TASKS.md#15-v2-端到端真实业务验证总结) |
| `5a9ddaf` | fix(llm+prompt): V2 P0 bug 4 — composer 输出协议无关 LLMContentBlock IR + provider 各自翻译 tool_use/tool_result — KYC 第 5 轮（openai-compat + gpt-5.3-chat-latest）暴露：composer.extractText 把 assistant tool_call 序列化为 "→ tool_call: Bash({...})" 字符串、tool_result 包成 user role，**只对 Anthropic 凑效**（Claude 凭文本能推断）；OpenAI 协议要求 message-level `tool_calls` 字段 + `role:'tool' + tool_call_id` 独立 message。所以 openai-compat 端 LLM 看不到自己真发过 tool_call → 22 轮 stop loop 反复问"请贴代码"。修复：types.ts/services.ts 引入 LLMContentBlock=text\|tool_call\|tool_result IR；anthropic.ts toAnthropicMessage 翻译为 {type:'tool_use',id,name,input} / {type:'tool_result',tool_use_id,content,is_error}；openai-compat.ts appendOpenAIMessages 翻译为 assistant message+tool_calls[] + 独立 role:'tool' message；composer/prompt-minimal 输出结构化 block 保留 callId。+ 13 个回归测试覆盖两端协议翻译。319 pass。 | [ALPHA_TASKS §16.2 bug 4](./ALPHA_TASKS.md#162-引擎层发现的-p0-bug) |
| `2af37af` | fix(prompt): DEFAULT_RECENT 10 → 30 + 加"防反复探索不动手"规则 — 修第 5 轮失忆循环 — bug 4 修后 LLM 真调 40 次 Bash 但 100% 只读，反复 cat KycController.java 8 次（每次以为自己第一次读）→ budget_iterations 用完。根因：每工具往返 2 message，10 条 = 5 对工具历史，第 6 轮起 LLM 真失忆。修复：composer.ts/prompt-minimal.ts DEFAULT_RECENT 10→30（≈15 工具往返历史）；composer.ts 协作规则加 "🚫 防反复探索不动手"段（每文件最多 cat/grep 3 次 / 看完关键文件下一步必须写 / 看到自己重复读 = 失忆信号立即动手 / 不要每轮重新 cat）。 | [ALPHA_TASKS §16.3](./ALPHA_TASKS.md#163-p1-调优-commit-2af37af) |
| `cd6ec32` | fix(runtime): V2 P0 #2 — emit_deliverable 拦"零业务工具调用" + 借鉴 OpenClaw/hermes 防谎报 — DEFAULT_RECENT 调优后 gpt-5.3-chat-latest 暴露新谎报模式：**第 1 轮 LLM 调用直接 emit_deliverable**，把 Java 代码贴在 contentText 里假装完成（0 个 Bash 调用），绕过 §15 P0 #1 修复（无"上一个失败"可拦）。对照 OpenClaw（架构上没有 emit_deliverable 工具，用 push-based 完成）+ hermes-agent（kanban_complete 严格前置 + TOOL_USE_ENFORCEMENT_GUIDANCE prompt 针对 gpt/qwen 自动注入）的防御策略，套 hermes "前置检查"思路：加 countBusinessToolCalls(repos, threadId) helper（系统工具不写 tool_call message，等价于"业务工具+spawn_employee"次数）；emit_deliverable case 加 "businessToolCalls===0 → block + 写 system error" 守卫；子工单（parentRequirementId!=null）跳过守卫。测试调整：12 个旧 mock 简化测试加 setup.primeBusinessTool() helper。320 pass。 | [ALPHA_TASKS §16.2 bug 5](./ALPHA_TASKS.md#162-引擎层发现的-p0-bug) |
| `2b409f4` | fix(llm): V2 P0 #3 — openai-compat orphan tool_result 防御（chat history 窗口切断时不再 400） — KYC 第 7 轮（emit_deliverable 守卫修后 LLM 真改 KycController.java 加 redirectUrl 守卫 + 试 mvn）跑到 ~14 轮报 `400 Invalid parameter: messages with role 'tool' must be a response to a preceeding message with 'tool_calls'`。根因：composer.tailByThread(threadId, DEFAULT_RECENT=30) 切窗口时把对应 tool_call 切走，留下 orphan tool_result message；openai-compat appendOpenAIMessages 直接转 role='tool' 发出 → OpenAI 协议要求 role:'tool' 前必须紧跟含 tool_calls 的 assistant。修复：buildBody 维护 knownToolCallIds Set 追踪本轮已发出 tool_call ID；appendOpenAIMessages 收到 tool_result 时若 callId 不在 set 里 → 不发 role:'tool'，改写成 user 文本 "[tool_result of <id>]\n<output>"，既保留上下文又不违反协议。Anthropic 不受影响（tool_use_id 允许孤立）。 | [ALPHA_TASKS §16.2 bug 6](./ALPHA_TASKS.md#162-引擎层发现的-p0-bug) |
| `562207c` | docs: ALPHA_TASKS §16 — V2 第二轮端到端验证总结（openai-compat + gpt-5.3-chat-latest） — 记录本会话从"openai-compat 完全跑不通业务"修到"LLM 真改 KycController + mvn 验证"全过程：§16.1 LLM 配置陷阱（Unicode 连字符 + codex 模型不支持 tool-use）；§16.2 3 个 P0 引擎 bug（bug 4/5/6 详细根因+修复+OpenClaw/hermes 对照）；§16.3 P1 调优；§16.4 LLM 行为前后对比表（7 轮迭代演进 0→真改代码）；§16.5 6 条实战教训（含 OpenAI/Anthropic 协议严格度差异、架构对照价值）；§16.6 收尾（320 pass + 业务真改代码）。 | [ALPHA_TASKS §16](./ALPHA_TASKS.md#16-v2-第二轮端到端验证-openai-compat--gpt-53-chat-latest2026-05-28-完成) |
